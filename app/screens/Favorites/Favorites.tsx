import { View, Text, SafeAreaView } from 'react-native';
import React from 'react';

const Favorites = () => {
  return (
    <SafeAreaView>
      <Text>Favorites</Text>
    </SafeAreaView>
  );
};

export default Favorites;
